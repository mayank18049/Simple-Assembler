# Dict about assembly opcode as key and machine opcode and other descriptions as values(type of instruction)
# Provides functions to check if the provided assembly instrction is correct or not.
# Calls error-gen if required 